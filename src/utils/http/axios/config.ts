/** 请求头中 token 的 key（与当前项目一致） */
export const HEADER_TOKEN = 'X-Access-Token';
/** 请求头中时间戳随机数的 key（GET 请求防缓存） */
export const HEADER_TIMESTAMP_RANDOM = 'X-TIMESTAMP';
/** Authorization 头 */
export const HEADER_AUTH = 'Authorization';
